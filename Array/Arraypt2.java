package Array;
import java.util.*;

public class Arraypt2 {
    /*Array is a fixed length data structure whereas ArrayList
    is a variable length Collection class. We cannot change length of array once
    created in Java but ArrayList can be changed.
    We cannot store primitives in ArrayList, it can only store objects.
    But array can contain both primitives and objects in Java.*/

    public static void main(String[] args) {
        String[] strings = new String[2];

        strings[0] = "Demian, Herman Hesse";
    }

}
